<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Khanhsan_Cnpm"
        tools:targetApi="31">

        <activity android:name=".LoginQuanLyActivity" />
        <activity android:name=".LoginNhanVienActivity" />

        <!-- ✅ ĐÃ THÊM android:exported -->
        <activity
            android:name=".TrangchinhActivity"
            android:exported="true" />

        <activity android:name=".Trangchinh_QL_Activity"/>
        <activity android:name=".DatphongActivity" />
        <activity android:name=".SodophongActivity" />

        <activity
            android:name=".ChonvaitroActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
